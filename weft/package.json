{
  "name": "weft",
  "private": true,
  "version": "0.0.0",
  "description": "Weft v0 — post-platform communications channel (scaffold)",
  "license": "SEE LICENSE IN ../LICENSE",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0"
  }
}
